import { GridBerryRowsPipe } from './grid-berry-rows.pipe';

describe('GridBerryRowsPipe', () => {
  it('create an instance', () => {
    const pipe = new GridBerryRowsPipe();
    expect(pipe).toBeTruthy();
  });
});
